events { }
http {
  upstream backend {
    server bloom:8080;
  }

  server {
    listen 80;
    access_log off;

    location / {
      proxy_pass http://backend;
      proxy_set_header Bloom-Request-Shard 0;
    }
  }
}

